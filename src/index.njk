---
layout: "base.njk"
title: "Inicio"
eleventyComputed:
  metadata:
    title: "{{ title }}"
    img: "https://v1.screenshot.11ty.dev/{{ meta.baseUrl | formatUrl}}{{ page.url | formatUrl }}/opengraph/"
---

<main>
  <h1 class="page-title">Prof. yonaiker Barreto</h1>
  <article>
    <header>Secciones</header>
    <div class="grid">
      {% for pagina in paginas %}
        <a href="{{ pagina.name | url }}" role="button">{{ pagina.title }}</a>
      {% endfor %}
    </div>
  </article>
</main>